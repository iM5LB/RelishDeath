# Admin Backups (Premium)

RelishDeath can store death backups and provide an admin GUI to recover them.

## Command

`/rd saves` (requires `relishdeath.admin.backups`)

## Storage

Backups are stored in SQLite:
- `plugins/RelishDeath/relishdeath.db`

